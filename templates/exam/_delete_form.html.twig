<form method="post" action="{{ path('app_exam_delete', {'id': exam.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cet examen ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exam.id) }}">
    <button  title="Supprimer du système" class="btn badge badge-danger">Supprimer</button>
</form>
