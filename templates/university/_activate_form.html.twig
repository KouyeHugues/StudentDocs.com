<form method="post" action="{{ path('app_university_activate', {'id': university.id}) }}" onsubmit="return confirm('Voulez vous activer cette université ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('activate' ~ university.id) }}">
    <button title="Activer pour le rendre public" class="btn badge badge-success">Activer</button>
</form>
